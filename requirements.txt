# ─────────────────────────────────────────────────────────────────────────────
# Language-conditioned dense-CLIP on BDD100K
#
# Tested with Python 3.10–3.12 on Linux with CUDA 12.x.
# The heavy dependency is open_clip_torch for the multilingual CLIP model.
# ─────────────────────────────────────────────────────────────────────────────

# Core numerical stack
numpy>=1.24
scipy>=1.11
pandas>=2.0

# Image handling
Pillow>=10.0
opencv-python-headless>=4.8

# Clustering / morphology (ported from OneMap)
scikit-image>=0.22
scikit-learn>=1.3

# Plotting (analysis + notebook)
matplotlib>=3.7
seaborn>=0.13

# OpenCLIP (multilingual XLM-R + ViT checkpoints)
# Verified compatible with open_clip_torch >= 2.20
open_clip_torch>=2.24

# PyTorch — install matching your CUDA version, e.g.:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16

# GPU energy monitoring (NVML)
nvidia-ml-py>=12.535; platform_system == "Linux"

# Progress bars
tqdm>=4.66
